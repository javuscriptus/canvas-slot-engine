// Палитра сборки ассетов — та же, что у игры.
//
// Этот файл больше ничего не объявляет: он переименовывает экспорты
// единственного источника, client/src/themes/sochi/palette.js. Раньше
// палитра лежала в двух местах, и любая правка золота в интерфейсе
// оставляла сгенерированные символы прежними — расхождение арта и UI
// накапливалось молча, потому что ни один тест не сравнивает картинку
// с константой в коде.
//
// Имена оставлены прежними (PALETTE, SOCHI, SOCHI_GEMS): в producers их
// два десятка употреблений, и переименование ради переименования только
// добавило бы шума в diff.

export {
  BASE as PALETTE,
  GEMS,
  ROYAL_PLATES,
  SCENERY as SOCHI,
  SCENERY_GEMS as SOCHI_GEMS
} from "../../client/src/themes/sochi/palette.js";
